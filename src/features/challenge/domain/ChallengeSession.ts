import type { AlarmChallengeResult } from "./AlarmChallengeResult";
export type ChallengeStatus = "idle" | "preparing-camera" | "camera-ready" | "capturing" | "processing" | "rejected" | "accepted" | "failed" | "cancelled";
export interface ChallengeSession { id: string; alarmId: string; targetShapeId: string; status: ChallengeStatus; attemptCount: number; startedAt: string; completedAt?: string; lastResult?: AlarmChallengeResult; }
export function createChallengeSession(alarmId: string, targetShapeId = "circle", now = new Date()): ChallengeSession { return { id: `${alarmId}-${now.getTime()}`, alarmId, targetShapeId, status: "idle", attemptCount: 0, startedAt: now.toISOString() }; }
export function transitionChallenge(session: ChallengeSession, status: ChallengeStatus, result?: AlarmChallengeResult): ChallengeSession { const attemptCount = status === "processing" ? session.attemptCount + 1 : session.attemptCount; return { ...session, status, attemptCount, lastResult: result ?? session.lastResult, completedAt: status === "accepted" || status === "cancelled" ? new Date().toISOString() : session.completedAt }; }
