import { Pressable, StyleSheet, Switch, Text, View } from "react-native";
import type { Alarm } from "@/features/alarms/domain/alarm";
import { formatRepeatDays, formatTime } from "@/utilities/format";
export function AlarmCard({ alarm, onToggle, onEdit, onDelete, onChallenge }: { alarm: Alarm; onToggle(enabled: boolean): void; onEdit(): void; onDelete(): void; onChallenge(): void }) { return <View style={styles.card}><Pressable accessibilityRole="button" accessibilityLabel={`Edit ${alarm.label || "alarm"}`} onPress={onEdit} style={styles.main}><Text style={styles.time}>{formatTime(alarm.hour, alarm.minute)}</Text><Text style={styles.label}>{alarm.label || "Alarm"}</Text><Text style={styles.detail}>{formatRepeatDays(alarm.repeatDays)} · Shape Photo Challenge</Text></Pressable><Switch value={alarm.enabled} onValueChange={onToggle} accessibilityLabel="Enable alarm" /><Pressable onPress={onChallenge} accessibilityRole="button"><Text style={styles.challenge}>Test challenge</Text></Pressable><Pressable onPress={onDelete} accessibilityRole="button"><Text style={styles.delete}>Delete</Text></Pressable></View>; }
const styles = StyleSheet.create({ card: { padding: 16, gap: 10, borderWidth: 1, borderColor: "#d2d6dc", borderRadius: 14, backgroundColor: "#fff" }, main: { gap: 3 }, time: { fontSize: 32, fontWeight: "700" }, label: { fontSize: 17, fontWeight: "600" }, detail: { color: "#59636e" }, challenge: { color: "#1d4ed8", fontWeight: "600", alignSelf: "flex-start", paddingVertical: 4 }, delete: { color: "#b42318", fontWeight: "600", alignSelf: "flex-start", paddingVertical: 4 } });
